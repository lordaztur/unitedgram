[project]
name = "unitedgram"
version = "1.1.0"
description = "Ponte bidirecional em tempo real entre um chat de tracker UNIT3D e um grupo/tópico do Telegram"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "-q"
